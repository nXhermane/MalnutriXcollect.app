import { verticalScale } from 'react-native-size-matters';
export const HEADER_EXPANDED_HEIGHT = verticalScale(300);
export const HEADER_COLLAPSED_HEIGHT = verticalScale(68);
export const SCROLL_THRESHOLD = verticalScale(70);
